# Generated by Ansible - DO NOT EDIT MANUALLY

# Flow Offloading table — processed on the forward hook, independent of
# table inet sing_box_tproxy (prerouting / output hooks).
# Tproxy'd traffic is diverted in prerouting and never reaches this hook,
# so every forwarded packet here has already been classified as bypass-proxy traffic.

table inet sing_box_offload {
	flowtable bypassed_flowtable {
		hook ingress priority 0
		devices = { {{ sing_box_nftables_flow_offload_interfaces | join(', ') }} }
	}

	chain forward_offload {
		type filter hook forward priority filter; policy accept;
		counter

		# Offload all forwarded TCP/UDP traffic.
		meta l4proto { tcp, udp } ct state new flow offload @bypassed_flowtable comment "flowtable offload for forwarded traffic"
	}
}
